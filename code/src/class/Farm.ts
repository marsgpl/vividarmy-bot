import { Config } from 'class/Config';

export class Farm {
    protected config: Config;

    constructor(config: Config) {
        this.config = config;
    }

    public async start() {
        //
    }
}
