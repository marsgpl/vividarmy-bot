import { Config } from 'types/local/Config';

export class TopWarBot {
    constructor(
        protected config: Config,
    ) {
        // this.log = _log.
    }

    public async start(): Promise<void> {
        // this.log('start');
    }
}
